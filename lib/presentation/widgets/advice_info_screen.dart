import 'package:flutter/material.dart';
class AdviceInfo extends StatelessWidget {
  const AdviceInfo({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Advice Info'),
      ),
    );
  }
}
